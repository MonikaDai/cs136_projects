
// merge_sort(a, len) sorts the elements of array a 
//   in ascending order
// requires: a is a valid array with len elements
// effects:  modifies a
// time:     O(nlogn), n = len
void merge_sort(int a[], int len);
